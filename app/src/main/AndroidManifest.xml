<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exploreca.tourfinder"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_exploreca"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MovieCategory.UpcomingMovies"></activity>
        <activity android:name=".MovieCategory.LatestMovies"></activity>
        <activity android:name=".MovieCategory.NowPlayingMovies"></activity>
        <activity android:name=".MovieCategory.PopularMovies"></activity>
        <activity android:name=".MovieCategory.TopRatedMovies"></activity>
        <activity android:name=".MovieDetailActivity"></activity>
        <activity android:name=".MyMovies"></activity>
        <activity android:name=".MovieTrailer"></activity>

    </application>

</manifest>